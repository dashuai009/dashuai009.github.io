<div
	id="banner"
	class="code-banner h-full transition duration-700 opacity-0 scale-105"
	role="img"
	aria-label="由 C++、Rust、WebAssembly 与渲染管线组成的极简编程图形"
>
	<div class="code-map" aria-hidden="true">
		<div class="code-line"></div>

		<div class="code-node node-cpp">
			<span class="code-glyph">{ }</span>
			<span class="code-label">C++</span>
		</div>

		<div class="code-node node-rust">
			<span class="code-glyph">::</span>
			<span class="code-label">RUST</span>
		</div>

		<div class="code-node node-wasm">
			<span class="code-glyph">&lt;/&gt;</span>
			<span class="code-label">WASM</span>
		</div>

		<div class="code-node node-render">
			<span class="render-glyph">
				<i></i><i></i><i></i>
			</span>
			<span class="code-label">PBRT</span>
		</div>
	</div>
</div>

<style>
	.code-banner {
		position: relative;
		display: grid;
		min-height: 18rem;
		place-items: center;
		overflow: hidden;
		background:
			radial-gradient(circle at 50% 56%, oklch(0.76 0.12 var(--hue) / 0.16), transparent 28%),
			linear-gradient(180deg, oklch(0.97 0.008 var(--hue)), oklch(0.93 0.02 var(--hue)));
		color: oklch(0.48 0.12 var(--hue));
	}

	:global(.dark) .code-banner {
		background:
			radial-gradient(circle at 50% 56%, oklch(0.62 0.13 var(--hue) / 0.18), transparent 30%),
			linear-gradient(180deg, oklch(0.19 0.018 var(--hue)), oklch(0.15 0.014 var(--hue)));
		color: oklch(0.79 0.1 var(--hue));
	}

	.code-banner::before {
		position: absolute;
		inset: 0;
		background-image:
			linear-gradient(to right, currentColor 1px, transparent 1px),
			linear-gradient(to bottom, currentColor 1px, transparent 1px);
		background-position: center;
		background-size: 4rem 4rem;
		content: "";
		mask-image: radial-gradient(ellipse 46% 62% at 50% 56%, black, transparent 78%);
		opacity: 0.055;
	}

	.code-map {
		position: relative;
		display: grid;
		width: min(46rem, calc(100% - 3rem));
		grid-template-columns: repeat(4, 1fr);
		align-items: center;
		padding-top: 3.75rem;
	}

	.code-line {
		position: absolute;
		left: 10%;
		right: 10%;
		top: 7.81rem;
		height: 1px;
		background: currentColor;
		opacity: 0.2;
	}

	.code-node {
		position: relative;
		display: grid;
		justify-items: center;
		gap: 0.8rem;
	}

	.code-node::before {
		position: absolute;
		top: 3.85rem;
		width: 0.42rem;
		height: 0.42rem;
		border: 2px solid color-mix(in oklab, currentColor 70%, transparent);
		border-radius: 50%;
		background: var(--page-bg);
		box-shadow: 0 0 0 0.38rem color-mix(in oklab, currentColor 9%, transparent);
		content: "";
	}

	.code-glyph,
	.render-glyph {
		display: grid;
		width: 4.7rem;
		height: 4.7rem;
		place-items: center;
		border: 1px solid color-mix(in oklab, currentColor 25%, transparent);
		border-radius: 1.25rem;
		background: color-mix(in oklab, var(--card-bg) 78%, transparent);
		box-shadow: 0 1rem 2.5rem color-mix(in oklab, currentColor 8%, transparent);
		font-family: "JetBrains Mono Variable", "SFMono-Regular", Consolas, monospace;
		font-size: 1.15rem;
		font-weight: 650;
		letter-spacing: -0.06em;
	}

	.code-label {
		margin-top: 0.62rem;
		font-family: "JetBrains Mono Variable", "SFMono-Regular", Consolas, monospace;
		font-size: 0.64rem;
		font-weight: 700;
		letter-spacing: 0.16em;
		opacity: 0.62;
	}

	.render-glyph {
		position: relative;
		grid-template-columns: repeat(3, 0.42rem);
		gap: 0.26rem;
	}

	.render-glyph::before,
	.render-glyph::after {
		position: absolute;
		width: 1.8rem;
		height: 1.8rem;
		border: 1px solid currentColor;
		content: "";
		opacity: 0.34;
		transform: rotate(45deg);
	}

	.render-glyph::after {
		width: 0.8rem;
		height: 0.8rem;
		opacity: 0.65;
	}

	.render-glyph i {
		width: 0.35rem;
		height: 0.35rem;
		border-radius: 50%;
		background: currentColor;
		opacity: 0.72;
	}

	@media (max-width: 640px) {
		.code-map {
			width: calc(100% - 1.5rem);
			padding-top: 3rem;
		}

		.code-glyph,
		.render-glyph {
			width: 3.8rem;
			height: 3.8rem;
			border-radius: 1rem;
			font-size: 0.95rem;
		}

		.code-line { top: 6.31rem; }
		.code-node::before { top: 3.1rem; }
		.code-label { font-size: 0.55rem; letter-spacing: 0.1em; }
	}

	@media (max-width: 420px) {
		.code-map { width: calc(100% - 0.75rem); }
		.code-glyph, .render-glyph { width: 3.25rem; height: 3.25rem; }
		.code-line { left: 8%; right: 8%; top: 5.86rem; }
		.code-node::before { top: 2.65rem; }
		.code-label { letter-spacing: 0.06em; }
	}

	@media (prefers-reduced-motion: reduce) {
		.code-banner { transition: none; }
	}
</style>
