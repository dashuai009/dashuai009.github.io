<div
	id="banner"
	class="code-banner h-full transition duration-700 opacity-0 scale-105"
	role="img"
	aria-label='C++23 Hello World 示例：使用 std::println 输出 “Hello, world!”'
>
	<div class="terminal" aria-hidden="true">
		<div class="terminal-bar">
			<div class="window-dots"><i></i><i></i><i></i></div>
			<span class="file-name">hello.cpp</span>
			<span class="language">C++23</span>
		</div>

		<div class="editor">
			<div class="code-row">
				<span class="line-number">1</span>
				<span class="typed typed-include"><b>#include</b> <em>&lt;print&gt;</em></span>
			</div>
			<div class="code-row blank-row"><span class="line-number">2</span></div>
			<div class="code-row">
				<span class="line-number">3</span>
				<span class="typed typed-main"><b>int</b> main() {`{`}</span>
			</div>
			<div class="code-row">
				<span class="line-number">4</span>
				<span class="typed typed-print">&nbsp;&nbsp;&nbsp;&nbsp;<em>std::println</em>(<q>Hello, world!</q>);</span>
			</div>
			<div class="code-row">
				<span class="line-number">5</span>
				<span class="typed typed-brace">}</span>
			</div>
		</div>

		<div class="output-line">
			<span class="prompt">›</span>
			<span>Hello, world!</span>
		</div>
	</div>
</div>

<style>
	.code-banner {
		position: relative;
		display: grid;
		min-height: 18rem;
		place-items: center;
		overflow: hidden;
		background:
			radial-gradient(circle at 50% 58%, oklch(0.76 0.12 var(--hue) / 0.14), transparent 32%),
			linear-gradient(180deg, oklch(0.97 0.008 var(--hue)), oklch(0.93 0.02 var(--hue)));
		color: oklch(0.37 0.055 var(--hue));
	}

	:global(.dark) .code-banner {
		background:
			radial-gradient(circle at 50% 58%, oklch(0.62 0.13 var(--hue) / 0.17), transparent 34%),
			linear-gradient(180deg, oklch(0.19 0.018 var(--hue)), oklch(0.15 0.014 var(--hue)));
		color: oklch(0.88 0.025 var(--hue));
	}

	.code-banner::before {
		position: absolute;
		inset: 0;
		background-image:
			linear-gradient(to right, currentColor 1px, transparent 1px),
			linear-gradient(to bottom, currentColor 1px, transparent 1px);
		background-position: center;
		background-size: 4rem 4rem;
		content: "";
		mask-image: radial-gradient(ellipse 48% 70% at 50% 58%, black, transparent 80%);
		opacity: 0.045;
	}

	.terminal {
		position: relative;
		width: min(35rem, calc(100% - 2rem));
		overflow: hidden;
		border: 1px solid color-mix(in oklab, var(--primary) 18%, transparent);
		border-radius: 1.1rem;
		background: color-mix(in oklab, var(--card-bg) 92%, transparent);
		box-shadow: 0 1.5rem 4rem color-mix(in oklab, var(--primary) 13%, transparent);
		transform: translateY(2.2rem);
		backdrop-filter: blur(14px);
	}

	.terminal-bar {
		display: grid;
		height: 2.65rem;
		grid-template-columns: 1fr auto 1fr;
		align-items: center;
		padding: 0 1rem;
		border-bottom: 1px solid var(--line-divider);
		background: color-mix(in oklab, var(--btn-regular-bg) 58%, transparent);
		font-family: "JetBrains Mono Variable", "SFMono-Regular", Consolas, monospace;
		font-size: 0.65rem;
	}

	.window-dots {
		display: flex;
		gap: 0.35rem;
	}

	.window-dots i {
		width: 0.42rem;
		height: 0.42rem;
		border-radius: 50%;
		background: currentColor;
		opacity: 0.16;
	}

	.file-name { font-weight: 650; opacity: 0.58; }

	.language {
		justify-self: end;
		color: var(--primary);
		font-weight: 750;
		letter-spacing: 0.08em;
	}

	.editor {
		padding: 1.05rem 1.25rem 0.8rem;
		font-family: "JetBrains Mono Variable", "SFMono-Regular", Consolas, monospace;
		font-size: clamp(0.68rem, 2.2vw, 0.86rem);
		line-height: 1.7;
	}

	.code-row {
		display: flex;
		min-height: 1.7em;
		align-items: baseline;
	}

	.line-number {
		width: 2.2rem;
		flex: 0 0 2.2rem;
		padding-right: 0.85rem;
		color: currentColor;
		text-align: right;
		opacity: 0.22;
		user-select: none;
	}

	.typed {
		display: inline-block;
		width: 0;
		overflow: hidden;
		border-right: 0.1em solid transparent;
		white-space: nowrap;
	}

	.typed b {
		color: oklch(0.64 0.17 335);
		font-weight: 650;
	}

	.typed em {
		color: var(--primary);
		font-style: normal;
	}

	.typed q {
		color: oklch(0.62 0.13 155);
		quotes: '"' '"';
	}

	:global(.dark) .typed b { color: oklch(0.76 0.14 335); }
	:global(.dark) .typed q { color: oklch(0.76 0.12 155); }

	.typed-include {
		animation: type-include 11s steps(16, end) infinite, caret-include 11s step-end infinite;
	}

	.typed-main {
		animation: type-main 11s steps(12, end) infinite, caret-main 11s step-end infinite;
	}

	.typed-print {
		animation: type-print 11s steps(34, end) infinite, caret-print 11s step-end infinite;
	}

	.typed-brace {
		animation: type-brace 11s step-end infinite, caret-brace 11s step-end infinite;
	}

	.output-line {
		display: flex;
		align-items: center;
		gap: 0.65rem;
		min-height: 2.55rem;
		padding: 0.6rem 1.25rem;
		border-top: 1px solid var(--line-divider);
		background: color-mix(in oklab, var(--btn-regular-bg) 38%, transparent);
		font-family: "JetBrains Mono Variable", "SFMono-Regular", Consolas, monospace;
		font-size: 0.72rem;
		opacity: 0;
		animation: show-output 11s ease infinite;
	}

	.prompt { color: var(--primary); font-size: 1rem; font-weight: 800; }

	@keyframes type-include {
		0%, 4% { width: 0; }
		19%, 96% { width: 16ch; }
		98%, 100% { width: 0; }
	}

	@keyframes caret-include {
		0%, 3%, 20%, 100% { border-right-color: transparent; }
		4%, 19% { border-right-color: currentColor; }
	}

	@keyframes type-main {
		0%, 22% { width: 0; }
		36%, 96% { width: 12ch; }
		98%, 100% { width: 0; }
	}

	@keyframes caret-main {
		0%, 21%, 37%, 100% { border-right-color: transparent; }
		22%, 36% { border-right-color: currentColor; }
	}

	@keyframes type-print {
		0%, 39% { width: 0; }
		70%, 96% { width: 34ch; }
		98%, 100% { width: 0; }
	}

	@keyframes caret-print {
		0%, 38%, 71%, 100% { border-right-color: transparent; }
		39%, 70% { border-right-color: currentColor; }
	}

	@keyframes type-brace {
		0%, 74% { width: 0; }
		77%, 96% { width: 1ch; }
		98%, 100% { width: 0; }
	}

	@keyframes caret-brace {
		0%, 73%, 78%, 100% { border-right-color: transparent; }
		74%, 77% { border-right-color: currentColor; }
	}

	@keyframes show-output {
		0%, 81% { opacity: 0; transform: translateY(0.2rem); }
		85%, 96% { opacity: 0.72; transform: translateY(0); }
		98%, 100% { opacity: 0; transform: translateY(0); }
	}

	@media (max-width: 520px) {
		.terminal { width: calc(100% - 1rem); border-radius: 0.9rem; transform: translateY(1.8rem); }
		.terminal-bar { padding-inline: 0.8rem; }
		.editor { padding-inline: 0.65rem; }
		.line-number { width: 1.75rem; flex-basis: 1.75rem; padding-right: 0.55rem; }
		.output-line { padding-inline: 0.9rem; }
	}

	@media (prefers-reduced-motion: reduce) {
		.code-banner { transition: none; }
		.typed { border-right-color: transparent; animation: none; }
		.typed-include { width: 16ch; }
		.typed-main { width: 12ch; }
		.typed-print { width: 34ch; }
		.typed-brace { width: 1ch; }
		.output-line { opacity: 0.72; animation: none; }
	}
</style>
